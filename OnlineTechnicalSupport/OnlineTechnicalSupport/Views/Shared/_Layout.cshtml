<!DOCTYPE html>
<html>
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
    @Styles.Render("~/Content/datepickerCSS")
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
</head>
<body>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @Scripts.Render("~/bundles/signalr")
    @Scripts.Render("~/bundles/datepickerJS")
    @RenderSection("scripts", required: false)

    <script src="https://unpkg.com/gijgo@1.9.13/js/gijgo.min.js" type="text/javascript"></script>
    <link href="https://unpkg.com/gijgo@1.9.13/css/gijgo.min.css" rel="stylesheet" type="text/css" />

    @Html.Raw(TempData["msg"])

    <nav class="navbar fixed-top navbar-expand-sm navbar-dark bg-primary">
        <div class="container-fluid">
            @Html.ActionLink("BT Destek", "Index", "Home", new { area = "" }, new { @class = "navbar-brand pl-2 pr-2" })
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarText">
                <ul class="navbar-nav">
                    @*<li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle text-white" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Masaüstü Bilgisayar
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                            <a class="dropdown-item" href="#">Action</a>
                            <a class="dropdown-item" href="#">Another action</a>
                            <a class="dropdown-item" href="#">Something else here</a>
                        </div>
                    </li>*@
                </ul>
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle text-white" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            <i class="fa fa-user" aria-hidden="true"></i>
                        </a>
                        <div class="dropdown-menu text-left" aria-labelledby="navbarDropdownMenuLink" style="left:unset; right:0px;">
                            <a class="dropdown-item" href="~/Security/Login" style="@(ViewBag.User == null ? "display:block;" : "display:none;")">Oturum Aç</a>
                            <a class="dropdown-item" href="~/Home/MyAccount" style="@(ViewBag.User == null ? "display:none;" : "display:block;")">Hesabım</a>
                            <a class="dropdown-item" href="~/Security/Logout" style="@(ViewBag.User == null ? "display:none;" : "display:block;")">Oturumu Kapat</a>
                        </div>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div>
        @RenderBody()
        <hr />
        <footer class="container">
            <p class="text-center">&copy; @DateTime.Now.Year - ASP.NET Uygulamam</p>
        </footer>
    </div>

    @*<script src="~/signalr/hubs" type="text/javascript"></script>
        <script type="text/javascript">
            $(function () {
                var msg = $.connection.chatHub;
                msg.client.displayMessages = function () {
                    getData();
                };
                $.connection.hub.start();
                getData();
            });
            function getData() {
                var $tbl = $('#chatArea');
                $.ajax({
                    url: $('#GetMessages').val(),
                    type: 'GET',
                    data: { code: location.search.split('code=')[1] },
                    datatype: 'json',
                    success: function (data) {
                        $tbl.empty();
                        $.each(data.msgList, function (i, model) {
                            var $isManager = model.ManagerId;
                            if (model == null) {
                                $tbl.append(
                                    '<div class="lcorners ">' + 'Lütfen bekleyiniz.' + '</div>'
                                );
                            }
                            else if ($isManager != "") {
                                $tbl.append(

                                    '<div class="bg-primary" style="float:left; padding:5px; background-color:white; border-radius: 0px 15px 15px 15px;">' +

                                    '<p style="max-width:250px; color:crimson;">' + model.SenderName + '</p>' + '<br>' +
                                    '<p style="max-width:250px;">' + model.TextContent + '</p>' + '<br>' +
                                    '<p style="max-width:250px;">' + model.Date + '</p>' +

                                    '</div>'

                                );
                            }
                            else {
                                $tbl.append(

                                    '<div class="bg-light text-dark p-2 mt-3 w-75" style="border-radius: 0px 15px 15px 15px;">' +

                                    '<small class="w-100 font-weight-bold border border-dark">' + model.SenderName + ' - ' +  model.Date + '</small>' + '<br/>' +
                                    '<small class="w-100 text-break border border-dark">' + model.TextContent + '</small>' +

                                    '</div>'

                                );
                            }
                        });
                    }
                });
            }
        </script>*@

</body>
</html>
